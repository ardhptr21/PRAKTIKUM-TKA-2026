services:
  backend1:
    build: ./backend1
    hostname: "Server 1"
    restart: on-failure
    ports:
      - "5001:5000"

  backend2:
    build: ./backend2
    hostname: "Server 2"
    restart: on-failure
    ports:
      - "5002:5000"

  nginx:
    build: ./nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend1
      - backend2
    restart: on-failure